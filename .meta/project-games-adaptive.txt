ПРАВИЛА ДЛЯ НЕЙРОСЕТЕЙ ПО РЕПОЗИТОРИЮ vi3na1bita-games:

- Ты — Senior Lead Architect и Code Optimizer с экспертным знанием JavaScript, PWA, iframe sandbox, postMessage bridge, CSS/Canvas performance и мобильной оптимизации.
- Язык ответов: по умолчанию RU. Английский — только если явно попросят или в именах/терминах.
- Всегда начинай с полного анализа приложения.
- Всегда указывай точные пути файлов.
- Любой код выводи только в тройных бэктиках с указанием языка.
- Учитывай, что проект редактируется через web-интерфейс github.com, без локального запуска.
- Формат изменений: -> ФАЙЛ: путь -> НАЙТИ: [фрагмент дословно] -> ЗАМЕНИТЬ НА: [полный новый блок].
- Не генерируй файлы .meta/project-games-full.txt и .meta/project-games-adaptive.txt целиком в ответах.
- Если нужен новый файл — присылай полный файл.
- Если нужен патч — присылай минимальный точечный блок.
- Стиль кода: vanilla JavaScript ESM, 2 пробела, без лишних зависимостей.

АРХИТЕКТУРНЫЕ ПРАВИЛА GAME CENTER:

- vi3na1bita-games — это изолированный micro-app для iframe внутри основного приложения vi3na1bita-music.
- Основное приложение остаётся владельцем плеера, авторизации, профиля, статистики, достижений, backup/sync.
- Game Center не имеет права вызывать play, pause, stop, mute, volume, seek, next, prev.
- Game Center не должен создавать AudioContext, WebAudio, Howler, <audio> или игровые звуки без отдельного parent-mediated слоя.
- Game Center не должен читать OAuth token, localStorage, IndexedDB, metaDB или parent.window.
- Любая связь с основным приложением — только через postMessage bridge.
- Разрешённые исходящие сообщения: GC_READY, GC_REQUEST_SNAPSHOT, GC_DOOR_CLICKED, GC_CLOSE и будущие write-limited события только после валидации parent-side.
- Запрещённые исходящие сообщения: любые команды управления музыкой, громкостью, плейлистом, избранным, качеством.
- Snapshot от parent считается read-only.
- При standalone-запуске приложение должно работать как preview без parent bridge.
- Ошибка Game Center не должна ломать основное приложение и не должна мешать воспроизведению музыки.
- Для iOS приоритет: не мешать фоновому воспроизведению и lockscreen playback основного приложения.
- Первая визуальная стратегия: CSS/2.5D panorama без WebGL и без heavy runtime.
- Любой RAF/timer должен останавливаться или деградировать при document.hidden.
- Не добавлять service worker в game repo на раннем этапе, чтобы не конфликтовать с root SW основного приложения.
- Не создавать папку Games/ внутри этого репозитория: корень репозитория деплоится в bucket prefix /Games/.

Название репозитория: vi3na1bita-games
Адрес репозитория: https://github.com/apel-s-in/vi3na1bita-games
Назначение: изолированный Game Center micro-app для основного приложения vi3na1bita-music.
Публичный путь после деплоя: https://vi3na1bita.website.yandexcloud.net/Games/
Проект делается и обслуживается средствами https://github.com/ (GitHub Pages + GitHub Actions + Yandex Object Storage).

СТРУКТУРА ПРОЕКТА:
vi3na1bita-games/
├── .git/
├── .github/
│   └── workflows/
│       ├── deploy.yml
│       └── generate-context.yml
├── .meta/
├── assets/
│   └── tower/
│       ├── 1
│       ├── bg.webp
│       └── tower.webp
├── previews/
│   └── tournaments-wall.html
├── src/
│   └── app.js
├── .nojekyll
├── ai-rules.txt
├── generate-context.js
├── index.html
├── README.md
└── styles.css

Сгенерировано: 2026-05-11 14:25:28 UTC

//=================================================
// FILE: /.github/workflows/deploy.yml
name: Deploy Game Center to Yandex Cloud S3

on:
  push:
    branches: [main, master]
    paths-ignore:
      - '.meta/**'
      - '.github/workflows/generate-context.yml'
  workflow_dispatch:

concurrency:
  group: yc-s3-games-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy repo root to /Games/
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.YC_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.YC_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ru-central1
          BUCKET: vi3na1bita
          PREFIX: Games
        run: |
          set -euo pipefail

          aws --endpoint-url=https://storage.yandexcloud.net s3 sync ./ "s3://$BUCKET/$PREFIX/" \
            --delete \
            --only-show-errors \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude ".meta/*" \
            --exclude ".meta/**" \
            --exclude ".DS_Store" \
            --exclude "README.md" \
            --exclude "ai-rules.txt" \
            --exclude "generate-context.js"

          aws --endpoint-url=https://storage.yandexcloud.net s3 cp ./index.html "s3://$BUCKET/$PREFIX/index.html" \
            --only-show-errors \
            --cache-control "no-cache, no-store, must-revalidate" \
            --content-type "text/html; charset=utf-8"

          aws --endpoint-url=https://storage.yandexcloud.net s3 cp ./styles.css "s3://$BUCKET/$PREFIX/styles.css" \
            --only-show-errors \
            --cache-control "public, max-age=300" \
            --content-type "text/css; charset=utf-8"

          aws --endpoint-url=https://storage.yandexcloud.net s3 cp ./src/app.js "s3://$BUCKET/$PREFIX/src/app.js" \
            --only-show-errors \
            --cache-control "public, max-age=300" \
            --content-type "application/javascript; charset=utf-8"

//=================================================
// FILE: /.github/workflows/generate-context.yml
name: Generate .meta games context

on:
  push:
    branches: [main, master]
    paths-ignore:
      - '.meta/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: games-context-${{ github.ref }}
  cancel-in-progress: true

jobs:
  meta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate Game Center context
        run: |
          node generate-context.js --mode=both --max-lines=20000
          ls -la .meta || true

      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "CI: generate games .meta context"
          branch: ${{ github.ref_name }}
          file_pattern: ".meta/project-games-*.txt"
          commit_user_name: github-actions[bot]
          commit_user_email: github-actions[bot]@users.noreply.github.com

//=================================================
// FILE: /ai-rules.txt
ПРАВИЛА ДЛЯ НЕЙРОСЕТЕЙ ПО РЕПОЗИТОРИЮ vi3na1bita-games:

- Ты — Senior Lead Architect и Code Optimizer с экспертным знанием JavaScript, PWA, iframe sandbox, postMessage bridge, CSS/Canvas performance и мобильной оптимизации.
- Язык ответов: по умолчанию RU. Английский — только если явно попросят или в именах/терминах.
- Всегда начинай с полного анализа приложения.
- Всегда указывай точные пути файлов.
- Любой код выводи только в тройных бэктиках с указанием языка.
- Учитывай, что проект редактируется через web-интерфейс github.com, без локального запуска.
- Формат изменений: -> ФАЙЛ: путь -> НАЙТИ: [фрагмент дословно] -> ЗАМЕНИТЬ НА: [полный новый блок].
- Не генерируй файлы .meta/project-games-full.txt и .meta/project-games-adaptive.txt целиком в ответах.
- Если нужен новый файл — присылай полный файл.
- Если нужен патч — присылай минимальный точечный блок.
- Стиль кода: vanilla JavaScript ESM, 2 пробела, без лишних зависимостей.

АРХИТЕКТУРНЫЕ ПРАВИЛА GAME CENTER:

- vi3na1bita-games — это изолированный micro-app для iframe внутри основного приложения vi3na1bita-music.
- Основное приложение остаётся владельцем плеера, авторизации, профиля, статистики, достижений, backup/sync.
- Game Center не имеет права вызывать play, pause, stop, mute, volume, seek, next, prev.
- Game Center не должен создавать AudioContext, WebAudio, Howler, <audio> или игровые звуки без отдельного parent-mediated слоя.
- Game Center не должен читать OAuth token, localStorage, IndexedDB, metaDB или parent.window.
- Любая связь с основным приложением — только через postMessage bridge.
- Разрешённые исходящие сообщения: GC_READY, GC_REQUEST_SNAPSHOT, GC_DOOR_CLICKED, GC_CLOSE и будущие write-limited события только после валидации parent-side.
- Запрещённые исходящие сообщения: любые команды управления музыкой, громкостью, плейлистом, избранным, качеством.
- Snapshot от parent считается read-only.
- При standalone-запуске приложение должно работать как preview без parent bridge.
- Ошибка Game Center не должна ломать основное приложение и не должна мешать воспроизведению музыки.
- Для iOS приоритет: не мешать фоновому воспроизведению и lockscreen playback основного приложения.
- Первая визуальная стратегия: CSS/2.5D panorama без WebGL и без heavy runtime.
- Любой RAF/timer должен останавливаться или деградировать при document.hidden.
- Не добавлять service worker в game repo на раннем этапе, чтобы не конфликтовать с root SW основного приложения.
- Не создавать папку Games/ внутри этого репозитория: корень репозитория деплоится в bucket prefix /Games/.

//=================================================
// FILE: /generate-context.js
/* eslint-disable no-console */
'use strict';

const fs = require('fs');
const path = require('path');

const argv = Object.fromEntries(process.argv.slice(2).map(a => {
  const [k, ...r] = a.replace(/^--/, '').split('=');
  return [k, r.join('=') === '' ? true : r.join('=')];
}));

const ROOT = path.resolve(argv.root || __dirname);
const META_DIR = path.resolve(argv['out-dir'] || path.join(ROOT, '.meta'));
const MODE = String(argv.mode || 'both').toLowerCase();
const MAX_LINES = Number(argv['max-lines'] || 20000);

if (!fs.existsSync(META_DIR)) fs.mkdirSync(META_DIR, { recursive: true });

const FULL_FILE = path.join(META_DIR, 'project-games-full.txt');
const ADAPTIVE_FILE = path.join(META_DIR, 'project-games-adaptive.txt');

const toUnix = p => String(p || '').replace(/\\/g, '/');
const SELF_FULL_REL = toUnix(path.relative(ROOT, FULL_FILE));
const SELF_ADAPT_REL = toUnix(path.relative(ROOT, ADAPTIVE_FILE));

const TEXT_EXTS = new Set([
  '.html', '.htm', '.css', '.js', '.mjs', '.cjs', '.ts', '.tsx',
  '.json', '.webmanifest', '.md', '.txt', '.yml', '.yaml', '.svg'
]);

const EXCLUDE_RAW = [
  '.git/**',
  '.meta/**',
  'node_modules/**',
  'dist/**',
  'build/**',
  'out/**',
  'coverage/**',
  '.cache/**',
  '.vscode/**',
  '.idea/**',
  '.husky/**',
  '.DS_Store',
  '**/*.log',
  '**/*.tmp',
  '**/*.temp',
  '**/*.bak',
  '**/*.orig',
  '**/*.rej',
  '**/*.map',
  '**/*.min.js',
  '**/*.min.css',
  'package-lock.json',
  'yarn.lock',
  'pnpm-lock.yaml',
  'qwen-code-*',
  '**/qwen-code-*',
  '.aider*',
  '**/.aider*',
  '.continue/**',
  '.copilot/**',
  'project-full*.txt',
  'project-adaptive*.txt',
  'project-games-full*.txt',
  'project-games-adaptive*.txt'
];

const BINARY_EXTS = new Set([
  '.png', '.jpg', '.jpeg', '.webp', '.avif', '.gif', '.ico',
  '.mp3', '.wav', '.ogg', '.m4a', '.mp4', '.webm',
  '.woff', '.woff2', '.ttf', '.otf',
  '.zip', '.7z', '.rar', '.gz', '.pdf'
]);

const PRIORITY = {
  critical: [
    /^index\.html?$/i,
    /^styles\.css$/i,
    /^src\/app\.js$/i,
    /^\.github\/workflows\/deploy\.ya?ml$/i,
    /^\.github\/workflows\/generate-context\.ya?ml$/i,
    /^generate-context\.js$/i,
    /^ai-rules\.txt$/i,
    /^README\.md$/i
  ],
  high: [
    /^src\/.*\.(js|mjs|ts)$/i,
    /^.*\.(css|html?)$/i,
    /^\.github\/workflows\/.*\.ya?ml$/i
  ],
  medium: [
    /^.*\.(js|mjs|cjs|ts|tsx|json|md|txt|ya?ml|css|html?|svg)$/i
  ]
};

const globToRegExp = p => {
  const hasPath = p.includes('/') || p.includes('**');
  const esc = p
    .replace(/[.+^${}()|[\]\\]/g, '\\$&')
    .replace(/\*\*/g, '___GLOBSTAR___')
    .replace(/\*/g, '[^/]*')
    .replace(/___GLOBSTAR___/g, '.*');
  return hasPath ? new RegExp(`^${esc}$`) : new RegExp(`(^|/)${esc}(/|$)`);
};

const EXCLUDES = EXCLUDE_RAW.map(globToRegExp);

const isExcluded = rel => {
  const u = toUnix(rel);
  if (!u || u === SELF_FULL_REL || u === SELF_ADAPT_REL) return true;
  return EXCLUDES.some(re => re.test(u));
};

const isTextFile = rel => {
  const ext = path.extname(rel).toLowerCase();
  if (BINARY_EXTS.has(ext)) return false;
  return TEXT_EXTS.has(ext);
};

const readText = rel => {
  try {
    return fs.readFileSync(path.join(ROOT, rel), 'utf8');
  } catch (e) {
    return `// read error: ${e.message}`;
  }
};

const countLines = s => (String(s || '').match(/\n/g) || []).length + (String(s || '').length ? 1 : 0);

const listAllEntries = (includeFiles = true) => {
  const out = [];
  const stack = [ROOT];

  while (stack.length) {
    const dir = stack.pop();
    let entries = [];
    try {
      entries = fs.readdirSync(dir, { withFileTypes: true });
    } catch {
      continue;
    }

    for (const item of entries) {
      const full = path.join(dir, item.name);
      const rel = toUnix(path.relative(ROOT, full)) || '.';
      if (isExcluded(rel)) continue;

      if (item.isDirectory()) {
        out.push({ rel, full, dir: true });
        stack.push(full);
      } else if (item.isFile() && includeFiles) {
        out.push({ rel, full, dir: false });
      }
    }
  }

  return out.sort((a, b) => a.dir !== b.dir ? (a.dir ? -1 : 1) : a.rel.localeCompare(b.rel));
};

const getPriority = rel => {
  const u = toUnix(rel);
  return Object.keys(PRIORITY).find(level => PRIORITY[level].some(re => re.test(u))) || 'low';
};

const readRepoUrl = () => {
  try {
    const cfg = path.join(ROOT, '.git', 'config');
    if (!fs.existsSync(cfg)) return '';
    return (fs.readFileSync(cfg, 'utf8').match(/url\s*=\s*(.+)\n/) || [])[1]?.trim() || '';
  } catch {
    return '';
  }
};

const renderTree = () => {
  const lines = ['СТРУКТУРА ПРОЕКТА:', `${path.basename(ROOT)}/`];

  const walk = (dir, prefix = '') => {
    let entries = [];
    try {
      entries = fs.readdirSync(dir, { withFileTypes: true });
    } catch {
      return;
    }

    const visible = entries
      .filter(x => !isExcluded(toUnix(path.relative(ROOT, path.join(dir, x.name)))))
      .sort((a, b) => a.isDirectory() !== b.isDirectory() ? (a.isDirectory() ? -1 : 1) : a.name.localeCompare(b.name));

    visible.forEach((x, i) => {
      const last = i === visible.length - 1;
      lines.push(`${prefix}${last ? '└── ' : '├── '}${x.name}${x.isDirectory() ? '/' : ''}`);
      if (x.isDirectory()) walk(path.join(dir, x.name), `${prefix}${last ? '    ' : '│   '}`);
    });
  };

  walk(ROOT);
  return lines.join('\n');
};

const headerBlock = () => {
  const rulesPath = path.join(ROOT, 'ai-rules.txt');
  const rules = fs.existsSync(rulesPath) ? `${fs.readFileSync(rulesPath, 'utf8').trim()}\n\n` : '';
  const repoName = String(argv['repo-name'] || path.basename(ROOT));
  const repoUrl = String(argv['repo-url'] || readRepoUrl() || 'https://github.com/apel-s-in/vi3na1bita-games');

  return `${rules}Название репозитория: ${repoName}
Адрес репозитория: ${repoUrl}
Назначение: изолированный Game Center micro-app для основного приложения vi3na1bita-music.
Публичный путь после деплоя: https://vi3na1bita.website.yandexcloud.net/Games/
Проект делается и обслуживается средствами https://github.com/ (GitHub Pages + GitHub Actions + Yandex Object Storage).

${renderTree()}

Сгенерировано: ${new Date().toISOString().replace('T', ' ').slice(0, 19)} UTC

`;
};

const fileBlock = rel => `//=================================================
// FILE: /${toUnix(rel)}
${readText(rel)}
`;

const generate = mode => {
  let out = headerBlock();
  let lines = countLines(out);

  const groups = listAllEntries(true)
    .filter(e => !e.dir && isTextFile(e.rel))
    .reduce((acc, e) => {
      acc[getPriority(e.rel)].push(e.rel);
      return acc;
    }, { critical: [], high: [], medium: [], low: [] });

  const order = mode === 'adaptive' ? ['critical', 'high', 'medium'] : ['critical', 'high', 'medium', 'low'];

  for (const level of order) {
    for (const rel of groups[level]) {
      const block = fileBlock(rel);
      const blockLines = countLines(block);
      if (mode === 'adaptive' && lines + blockLines > MAX_LINES) {
        return `${out}\n// ... adaptive context truncated by --max-lines=${MAX_LINES}\n`;
      }
      out += block;
      lines += blockLines;
    }
  }

  return out;
};

try {
  if (MODE === 'full' || MODE === 'both') {
    fs.writeFileSync(FULL_FILE, generate('full'), 'utf8');
    console.log(`✅ ${FULL_FILE}`);
  }

  if (MODE === 'adaptive' || MODE === 'both') {
    fs.writeFileSync(ADAPTIVE_FILE, generate('adaptive'), 'utf8');
    console.log(`✅ ${ADAPTIVE_FILE}`);
  }
} catch (e) {
  console.error('❌ context generation failed:', e);
  process.exit(1);
}

//=================================================
// FILE: /index.html
<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="color-scheme" content="dark">
  <meta name="theme-color" content="#07091a">
  <title>Башня Витрины · Game Center</title>
  <meta name="description" content="Музыкальный игровой центр Витрина Разбита">
  <link rel="stylesheet" href="./styles.css">
</head>
<body>
  <main class="bt-app" id="app">
    <section class="bt-scene" id="scene" aria-label="Башня Витрины">
      <canvas class="bt-particles" id="particles" aria-hidden="true"></canvas>

      <div class="bt-layer bt-layer-bg" data-depth="0.05">
        <img src="./assets/tower/bg.webp" alt="">
      </div>

      <svg class="bt-energy" viewBox="0 0 1000 1600" preserveAspectRatio="none" aria-hidden="true">
        <path class="bt-energy-line line-a" d="M780 120 C850 230 720 310 810 430 C920 590 760 700 850 850 C930 990 780 1150 900 1320" />
        <path class="bt-energy-line line-b" d="M410 260 C360 390 470 510 390 650 C300 790 440 910 350 1040 C270 1180 370 1320 290 1500" />
        <path class="bt-energy-line line-c" d="M520 560 C600 690 430 820 560 960 C690 1100 500 1240 610 1450" />
      </svg>

      <div class="bt-layer bt-layer-tower" data-depth="0.16">
        <img src="./assets/tower/tower.webp" alt="">
      </div>

      <div class="bt-layer bt-layer-fog bt-layer-fog-back" data-depth="0.09">
        <img src="./assets/tower/fog-back.webp" alt="">
      </div>

      <button class="bt-hotspot hotspot-melody is-primary" type="button" data-door="melody" data-depth="0.28">
        <span class="bt-portal"></span>
        <img class="bt-hotspot-icon" src="./assets/tower/icon-melody.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Зал Мелодий</b>
          <small>Угадай песню</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-words" type="button" data-door="words" data-depth="0.24">
        <span class="bt-portal"></span>
        <img class="bt-hotspot-icon" src="./assets/tower/icon-words.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Кузница Слов</b>
          <small>Собери строки</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-oracle" type="button" data-door="oracle" data-depth="0.32">
        <span class="bt-portal"></span>
        <img class="bt-hotspot-icon" src="./assets/tower/icon-oracle.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Оракул Текстов</b>
          <small>Викторины</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-heart is-heart" type="button" data-door="heart" data-depth="0.36">
        <img class="bt-hotspot-icon" src="./assets/tower/icon-heart.webp" alt="">
        <span class="bt-heart-pulse"></span>
        <span class="bt-hotspot-text">
          <b>Сердце Витрины</b>
          <small>Собери сердце из осколков</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-arena" type="button" data-door="arena" data-depth="0.3">
        <span class="bt-portal"></span>
        <img class="bt-hotspot-icon" src="./assets/tower/icon-arena.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Арена Турниров</b>
          <small>Соревнования</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-fame" type="button" data-door="fame" data-depth="0.2">
        <span class="bt-portal"></span>
        <img class="bt-hotspot-icon" src="./assets/tower/icon-crown.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Зал Славы</b>
          <small>Рейтинг игроков</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-daily" type="button" data-door="daily" data-depth="0.34">
        <img class="bt-hotspot-icon" src="./assets/tower/icon-daily.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Ежедневный вызов</b>
          <small>Игра дня</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-chest" type="button" data-door="chest" data-depth="0.34">
        <img class="bt-hotspot-icon" src="./assets/tower/icon-chest.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Сундук Осколков</b>
          <small>Награды</small>
        </span>
      </button>

      <button class="bt-hotspot hotspot-collection" type="button" data-door="collection" data-depth="0.25">
        <img class="bt-hotspot-icon" src="./assets/tower/icon-cards.webp" alt="">
        <span class="bt-hotspot-text">
          <b>Коллекция</b>
          <small>Карточки песен</small>
        </span>
      </button>

      <div class="bt-layer bt-layer-fog bt-layer-fog-front" data-depth="0.42">
        <img src="./assets/tower/fog-front.webp" alt="">
      </div>

      <header class="bt-topbar">
        <div class="bt-title">
          <h1>Башня Витрины</h1>
          <p>Музыкальный игровой центр</p>
        </div>

        <div class="bt-profile">
          <div class="bt-shards">
            <img src="./assets/tower/shard-red.webp" alt="">
            <span id="shards-count">1250</span>
            <small>осколков</small>
          </div>

          <div class="bt-avatar">
            <img src="./assets/tower/avatar.webp" alt="">
          </div>
        </div>
      </header>

      <footer class="bt-nav" aria-label="Навигация Game Center">
        <button class="bt-nav-item is-active" type="button" data-nav="games">
          <span>🎮</span>
          <b>Игры</b>
        </button>
        <button class="bt-nav-item" type="button" data-nav="tournaments">
          <span>🏆</span>
          <b>Турниры</b>
        </button>
        <button class="bt-nav-item" type="button" data-nav="rating">
          <span>📊</span>
          <b>Рейтинг</b>
        </button>
        <button class="bt-nav-item" type="button" data-nav="profile">
          <span>👤</span>
          <b>Профиль</b>
        </button>
      </footer>

      <div class="bt-bridge-pill" id="bridge-pill">bridge: wait</div>
      <div class="bt-toast" id="toast" hidden></div>
    </section>
  </main>

  <script type="module" src="./src/app.js"></script>
</body>
</html>

//=================================================
// FILE: /README.md
# vi3na1bita-games

Isolated Game Center micro-app for `vi3na1bita-music`.

Important:
- repository root is deployed into Yandex Object Storage prefix `/Games/`;
- do not create a root `Games/` folder inside this repo;
- no audio, no WebAudio, no direct access to parent app storage;
- communication with main app only through `postMessage` bridge.

//=================================================
// FILE: /src/app.js
// UID.001_(Playback safety invariant)_(game-app не управляет музыкой)_(нет audio/WebAudio/playback commands)
// UID.006_(Lazy isolated micro-app)_(работает внутри iframe или standalone preview)_(основной app загружает его по клику)
// UID.082_(Local truth vs external telemetry split)_(получаем только safe snapshot)_(не читаем localStorage/IndexedDB/token)
// UID.094_(No-paralysis rule)_(ошибка Game Center не ломает основное приложение)_(только postMessage + CSS/Canvas parallax)

const $ = id => document.getElementById(id);
const prefersReduced = window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches;

const state = {
  bridgeId: '',
  snapshot: null,
  activeDoor: '',
  pointerDown: false,
  targetX: 0,
  targetY: 0,
  lookX: 0,
  lookY: 0,
  startX: 0,
  startY: 0,
  dragX: 0,
  dragY: 0,
  targetDragX: 0,
  targetDragY: 0,
  raf: 0
};

const scene = $('scene');

const send = (type, payload = {}) => {
  if (!state.bridgeId || !window.parent || window.parent === window) return false;
  try {
    window.parent.postMessage({ kind: 'vitrina:game', bridgeId: state.bridgeId, type, payload }, '*');
    return true;
  } catch {
    return false;
  }
};

const showToast = text => {
  const toast = $('toast');
  if (!toast) return;

  toast.textContent = text;
  toast.hidden = false;

  clearTimeout(showToast.timer);
  showToast.timer = setTimeout(() => {
    toast.hidden = true;
  }, 1700);
};

const fmtNum = value => {
  const n = Number(value || 0);
  if (!Number.isFinite(n)) return '0';
  if (n >= 1000000) return `${(n / 1000000).toFixed(1).replace('.', ',')}M`;
  if (n >= 1000) return `${Math.round(n / 100) / 10}K`.replace('.', ',');
  return String(Math.round(n));
};

const applySnapshot = snapshot => {
  state.snapshot = snapshot || state.snapshot || {};

  const progress = state.snapshot?.progress || {};
  const user = state.snapshot?.user || {};

  const shardText = $('shards-count');
  if (shardText) shardText.textContent = fmtNum(progress.xp || 1250);

  const avatar = document.querySelector('.bt-avatar img');
  if (avatar && user.avatar) avatar.src = user.avatar;
};

const bindBridge = () => {
  window.addEventListener('message', e => {
    const d = e.data || {};
    if (d.kind !== 'vitrina:game-host') return;

    if (d.type === 'GC_INIT') {
      state.bridgeId = d.bridgeId || d.payload?.bridgeId || '';
      $('bridge-pill').textContent = state.bridgeId ? 'bridge: connected' : 'bridge: no id';
      applySnapshot(d.payload?.snapshot);
      send('GC_READY', { at: Date.now(), userAgent: navigator.userAgent.slice(0, 80) });
      send('GC_REQUEST_SNAPSHOT');
      return;
    }

    if (!state.bridgeId || d.bridgeId !== state.bridgeId) return;

    if (d.type === 'GC_SNAPSHOT' || d.type === 'GC_HOST_STATE') {
      applySnapshot(d.payload);
    }
  });

  if (window.parent === window) {
    $('bridge-pill').textContent = 'standalone';
    applySnapshot({
      user: { displayName: 'Standalone' },
      progress: { level: 1, xp: 1250, achievementsUnlocked: 0, achievementsTotal: 0 },
      player: { title: '' }
    });
  }
};

const bindHotspots = () => {
  document.querySelectorAll('.bt-hotspot').forEach(btn => {
    btn.addEventListener('click', e => {
      e.stopPropagation();

      const door = btn.dataset.door || 'unknown';
      const title = btn.querySelector('b')?.textContent || door;

      state.activeDoor = door;

      document.querySelectorAll('.bt-hotspot').forEach(x => {
        x.classList.toggle('is-active', x === btn);
      });

      showToast(`${title}: скоро откроется`);
      send('GC_DOOR_CLICKED', { door, at: Date.now() });

      spawnBurst(btn);
    });
  });

  document.querySelectorAll('.bt-nav-item').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.bt-nav-item').forEach(x => {
        x.classList.toggle('is-active', x === btn);
      });

      const nav = btn.dataset.nav || 'unknown';
      showToast(`Раздел: ${btn.textContent.trim()}`);
      send('GC_DOOR_CLICKED', { door: `nav:${nav}`, at: Date.now() });
    });
  });
};

const getPointerLook = e => {
  const r = scene.getBoundingClientRect();
  const cx = r.left + r.width / 2;
  const cy = r.top + r.height / 2;

  return {
    x: Math.max(-1, Math.min(1, (e.clientX - cx) / (r.width / 2))),
    y: Math.max(-1, Math.min(1, (e.clientY - cy) / (r.height / 2)))
  };
};

const bindParallaxInput = () => {
  if (!scene) return;

  scene.addEventListener('pointerdown', e => {
    if (e.target?.closest?.('button')) return;

    state.pointerDown = true;
    state.startX = e.clientX;
    state.startY = e.clientY;
    scene.setPointerCapture?.(e.pointerId);
  });

  scene.addEventListener('pointermove', e => {
    const look = getPointerLook(e);
    state.targetX = look.x;
    state.targetY = look.y;

    if (state.pointerDown) {
      state.targetDragX += (e.movementX || 0) * 0.18;
      state.targetDragY += (e.movementY || 0) * 0.08;
      state.targetDragX = Math.max(-90, Math.min(90, state.targetDragX));
      state.targetDragY = Math.max(-40, Math.min(40, state.targetDragY));
    }
  });

  const end = e => {
    state.pointerDown = false;
    try { scene.releasePointerCapture?.(e.pointerId); } catch {}
  };

  scene.addEventListener('pointerup', end);
  scene.addEventListener('pointercancel', end);

  window.addEventListener('deviceorientation', e => {
    if (!Number.isFinite(e.gamma) || !Number.isFinite(e.beta)) return;
    state.targetX = Math.max(-1, Math.min(1, e.gamma / 28));
    state.targetY = Math.max(-1, Math.min(1, (e.beta - 45) / 42));
  }, { passive: true });
};

const updateParallax = () => {
  state.lookX += (state.targetX - state.lookX) * 0.075;
  state.lookY += (state.targetY - state.lookY) * 0.075;
  state.dragX += (state.targetDragX - state.dragX) * 0.08;
  state.dragY += (state.targetDragY - state.dragY) * 0.08;

  document.querySelectorAll('[data-depth]').forEach(el => {
    const depth = Number(el.dataset.depth || 0);
    const x = state.dragX * depth + state.lookX * depth * 46;
    const y = state.dragY * depth + state.lookY * depth * 30;

    if (el.classList.contains('bt-hotspot')) {
      el.style.translate = `${x}px ${y}px`;
    } else {
      el.style.transform = `translate3d(${x}px,${y}px,0)`;
    }
  });
};

const createParticleEngine = canvas => {
  if (!canvas || prefersReduced) {
    return { start() {}, stop() {}, burst() {}, resize() {} };
  }

  const ctx = canvas.getContext('2d', { alpha: true });
  const particles = [];
  let width = 0;
  let height = 0;
  let dpr = 1;
  let raf = 0;
  let running = false;

  const rand = (min, max) => min + Math.random() * (max - min);

  const resize = () => {
    const r = canvas.getBoundingClientRect();
    dpr = Math.min(2, window.devicePixelRatio || 1);
    width = Math.max(1, Math.floor(r.width * dpr));
    height = Math.max(1, Math.floor(r.height * dpr));
    canvas.width = width;
    canvas.height = height;
  };

  const addParticle = (x, y, burst = false) => {
    particles.push({
      x,
      y,
      vx: rand(-0.35, 0.35) * dpr * (burst ? 6 : 1),
      vy: rand(-0.55, -0.05) * dpr * (burst ? 6 : 1),
      life: burst ? rand(28, 62) : rand(90, 220),
      maxLife: burst ? 62 : 220,
      size: rand(1, burst ? 4 : 2.6) * dpr,
      hue: Math.random() > 0.35 ? rand(186, 198) : rand(342, 354)
    });
  };

  const seed = () => {
    particles.length = 0;
    for (let i = 0; i < 95; i++) {
      addParticle(rand(0, width), rand(height * .12, height * .95));
    }
  };

  const burst = (clientX, clientY) => {
    const r = canvas.getBoundingClientRect();
    const x = (clientX - r.left) * dpr;
    const y = (clientY - r.top) * dpr;

    for (let i = 0; i < 38; i++) addParticle(x, y, true);
  };

  const frame = () => {
    if (!running || document.hidden) return;

    updateParallax();

    ctx.clearRect(0, 0, width, height);
    ctx.globalCompositeOperation = 'lighter';

    for (let i = particles.length - 1; i >= 0; i--) {
      const p = particles[i];

      p.life -= 1;
      p.x += p.vx;
      p.y += p.vy;
      p.vy += 0.006 * dpr;

      const alpha = Math.max(0, p.life / p.maxLife);
      ctx.fillStyle = `hsla(${p.hue}, 100%, 62%, ${alpha * .8})`;
      ctx.shadowBlur = 12 * dpr;
      ctx.shadowColor = `hsla(${p.hue}, 100%, 60%, .9)`;

      ctx.beginPath();
      ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
      ctx.fill();

      if (p.life <= 0 || p.y < -20 || p.y > height + 30) {
        particles.splice(i, 1);
      }
    }

    ctx.shadowBlur = 0;

    while (particles.length < 95) {
      addParticle(rand(0, width), height + rand(0, 80) * dpr);
    }

    raf = requestAnimationFrame(frame);
  };

  const start = () => {
    if (running || document.hidden) return;
    running = true;
    raf = requestAnimationFrame(frame);
  };

  const stop = () => {
    running = false;
    if (raf) cancelAnimationFrame(raf);
    raf = 0;
  };

  resize();
  seed();

  return { start, stop, burst, resize: () => { resize(); seed(); } };
};

let particleEngine = null;

const spawnBurst = el => {
  if (!particleEngine || !el) return;
  const r = el.getBoundingClientRect();
  particleEngine.burst(r.left + r.width / 2, r.top + r.height / 2);
};

const init = () => {
  bindBridge();
  bindHotspots();
  bindParallaxInput();

  particleEngine = createParticleEngine($('particles'));
  particleEngine.start();

  let resizeTimer = 0;
  window.addEventListener('resize', () => {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(() => particleEngine.resize(), 140);
  });

  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      particleEngine.stop();
    } else {
      particleEngine.resize();
      particleEngine.start();
    }
  });
};

init();

//=================================================
// FILE: /styles.css
:root{
  --bt-bg:#07091a;
  --bt-white:#ffffff;
  --bt-muted:#a7abc9;
  --bt-red:#ff3159;
  --bt-red2:#ff0f45;
  --bt-cyan:#25e8ff;
  --bt-blue:#167cff;
  --bt-panel:rgba(10,12,36,.74);
  --bt-border:rgba(123,130,210,.36);
}

*{box-sizing:border-box}

html,
body{
  width:100%;
  min-height:100%;
  margin:0;
  background:var(--bt-bg);
  color:var(--bt-white);
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
  overflow:hidden;
}

button{
  font:inherit;
  color:inherit;
  -webkit-tap-highlight-color:transparent;
}

.bt-app{
  position:fixed;
  inset:0;
  overflow:hidden;
  background:
    radial-gradient(circle at 50% 35%,rgba(25,125,255,.22),transparent 34%),
    linear-gradient(180deg,#090b22 0%,#040511 100%);
}

.bt-scene{
  position:absolute;
  inset:0;
  overflow:hidden;
  isolation:isolate;
  background:#07091a;
  touch-action:none;
  user-select:none;
}

.bt-particles{
  position:absolute;
  inset:0;
  z-index:32;
  width:100%;
  height:100%;
  pointer-events:none;
  mix-blend-mode:screen;
}

.bt-layer{
  position:absolute;
  inset:-6%;
  z-index:1;
  will-change:transform;
  pointer-events:none;
}

.bt-layer img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
}

.bt-layer-bg{
  z-index:1;
  inset:-8%;
}

.bt-layer-tower{
  z-index:10;
  inset:-7%;
}

.bt-layer-fog{
  z-index:35;
  opacity:.82;
  mix-blend-mode:screen;
}

.bt-layer-fog-back{
  z-index:8;
  opacity:.45;
  filter:blur(1px);
  animation:btFogBack 12s ease-in-out infinite alternate;
}

.bt-layer-fog-front{
  z-index:38;
  opacity:.75;
  animation:btFogFront 9s ease-in-out infinite alternate;
}

.bt-energy{
  position:absolute;
  inset:-4%;
  z-index:18;
  width:108%;
  height:108%;
  pointer-events:none;
  filter:drop-shadow(0 0 8px rgba(255,49,89,.65));
  mix-blend-mode:screen;
}

.bt-energy-line{
  fill:none;
  stroke:var(--bt-red);
  stroke-width:4;
  stroke-linecap:round;
  stroke-linejoin:round;
  opacity:.75;
  stroke-dasharray:40 28;
  animation:btEnergyFlow 3.4s linear infinite,btEnergyPulse 2.2s ease-in-out infinite;
}

.line-b{
  stroke:#21e8ff;
  opacity:.45;
  animation-duration:4.2s,2.8s;
}

.line-c{
  stroke:#ff3159;
  opacity:.58;
  animation-duration:3.1s,2.5s;
}

.bt-topbar{
  position:absolute;
  left:0;
  right:0;
  top:0;
  z-index:60;
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:14px;
  padding:calc(env(safe-area-inset-top) + 18px) 24px 18px;
  background:
    linear-gradient(180deg,rgba(7,8,25,.95),rgba(7,8,25,.82) 74%,rgba(7,8,25,0));
  border-bottom:1px solid rgba(129,135,221,.28);
  backdrop-filter:blur(8px);
}

.bt-title{
  min-width:0;
}

.bt-title h1{
  margin:0;
  font-size:clamp(34px,7vw,76px);
  line-height:.95;
  font-weight:900;
  letter-spacing:.2px;
  text-shadow:0 4px 18px rgba(0,0,0,.45);
}

.bt-title p{
  margin:12px 0 0;
  color:var(--bt-muted);
  font-size:clamp(20px,4.2vw,42px);
  line-height:1;
  font-weight:500;
}

.bt-profile{
  display:flex;
  align-items:center;
  gap:18px;
  flex-shrink:0;
}

.bt-shards{
  display:grid;
  grid-template-columns:auto auto;
  grid-template-rows:auto auto;
  align-items:center;
  column-gap:14px;
  min-width:190px;
  padding:12px 20px;
  border-radius:28px;
  background:rgba(19,21,58,.72);
  border:2px solid rgba(111,119,200,.5);
  box-shadow:inset 0 0 22px rgba(55,74,185,.25),0 0 24px rgba(255,49,89,.08);
}

.bt-shards img{
  grid-row:1 / 3;
  width:58px;
  height:58px;
  object-fit:contain;
  filter:drop-shadow(0 0 18px rgba(255,49,89,.55));
}

.bt-shards span{
  font-size:44px;
  line-height:.9;
  font-weight:900;
}

.bt-shards small{
  color:var(--bt-muted);
  font-size:22px;
  line-height:1;
  text-transform:uppercase;
  letter-spacing:.03em;
}

.bt-avatar{
  width:96px;
  height:96px;
  border-radius:26px;
  padding:5px;
  background:linear-gradient(135deg,#ff3159,#ff5a72);
  box-shadow:0 0 24px rgba(255,49,89,.3);
}

.bt-avatar img{
  width:100%;
  height:100%;
  display:block;
  object-fit:cover;
  border-radius:21px;
}

.bt-hotspot{
  position:absolute;
  z-index:30;
  width:260px;
  min-height:170px;
  padding:0;
  border:0;
  background:transparent;
  cursor:pointer;
  transform:translate3d(-50%,-50%,0);
  will-change:transform,filter;
}

.bt-hotspot::before{
  content:"";
  position:absolute;
  left:50%;
  top:48%;
  width:190px;
  height:190px;
  transform:translate(-50%,-50%);
  border-radius:50%;
  background:radial-gradient(circle,rgba(37,232,255,.2),rgba(37,232,255,.08) 42%,transparent 70%);
  filter:blur(4px);
  opacity:.75;
  transition:opacity .18s,transform .18s;
}

.bt-hotspot:hover::before,
.bt-hotspot.is-active::before{
  opacity:1;
  transform:translate(-50%,-50%) scale(1.08);
}

.bt-hotspot-icon{
  position:relative;
  z-index:3;
  display:block;
  width:138px;
  height:138px;
  object-fit:contain;
  margin:0 auto 8px;
  filter:
    drop-shadow(0 8px 4px rgba(0,0,0,.55))
    drop-shadow(0 0 18px rgba(37,232,255,.45));
  animation:btIconFloat 4.4s ease-in-out infinite;
}

.bt-hotspot-text{
  position:relative;
  z-index:4;
  display:block;
  text-align:center;
  text-shadow:
    0 4px 0 rgba(0,0,0,.75),
    0 0 9px rgba(0,0,0,.9);
}

.bt-hotspot-text b{
  display:block;
  font-size:clamp(22px,4vw,46px);
  line-height:1;
  font-weight:950;
  letter-spacing:.01em;
}

.bt-hotspot-text small{
  display:block;
  margin-top:8px;
  color:#c4c8df;
  font-size:clamp(17px,3vw,33px);
  line-height:1;
  font-weight:520;
}

.bt-portal{
  position:absolute;
  left:50%;
  top:42%;
  z-index:1;
  width:154px;
  height:154px;
  transform:translate(-50%,-50%);
  border-radius:50%;
  border:9px solid rgba(37,232,255,.88);
  box-shadow:
    0 0 18px rgba(37,232,255,.95),
    0 0 42px rgba(37,232,255,.62),
    inset 0 0 22px rgba(37,232,255,.65);
  animation:btPortalPulse 2.2s ease-in-out infinite,btPortalSpin 8s linear infinite;
}

.bt-portal::before,
.bt-portal::after{
  content:"";
  position:absolute;
  inset:-16px;
  border-radius:50%;
  border:2px solid rgba(37,232,255,.26);
}

.bt-portal::after{
  inset:18px;
  border-color:rgba(255,255,255,.18);
}

.bt-heart-pulse{
  position:absolute;
  left:50%;
  top:46%;
  z-index:1;
  width:210px;
  height:210px;
  transform:translate(-50%,-50%);
  border-radius:50%;
  background:radial-gradient(circle,rgba(255,49,89,.34),rgba(255,49,89,.12) 44%,transparent 72%);
  filter:blur(3px);
  animation:btHeartPulse 1.9s ease-in-out infinite;
}

.hotspot-melody{left:30%;top:29%}
.hotspot-words{left:56%;top:37%}
.hotspot-oracle{left:25%;top:54%}
.hotspot-heart{left:50%;top:60%}
.hotspot-arena{left:69%;top:57%}
.hotspot-fame{left:42%;top:74%}
.hotspot-daily{left:13%;top:78%}
.hotspot-chest{left:85%;top:78%}
.hotspot-collection{left:50%;top:88%}

.is-primary .bt-hotspot-icon{
  filter:
    drop-shadow(0 8px 4px rgba(0,0,0,.55))
    drop-shadow(0 0 26px rgba(37,232,255,.65));
}

.is-heart .bt-hotspot-icon{
  width:188px;
  height:188px;
  filter:
    drop-shadow(0 9px 5px rgba(0,0,0,.65))
    drop-shadow(0 0 30px rgba(255,49,89,.75));
}

.bt-nav{
  position:absolute;
  left:0;
  right:0;
  bottom:0;
  z-index:65;
  display:grid;
  grid-template-columns:repeat(4,1fr);
  min-height:118px;
  padding-bottom:env(safe-area-inset-bottom);
  background:rgba(9,10,35,.92);
  border-top:2px solid rgba(111,119,200,.36);
  backdrop-filter:blur(10px);
}

.bt-nav-item{
  position:relative;
  border:0;
  background:transparent;
  color:#aeb3d0;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:8px;
  cursor:pointer;
  overflow:hidden;
}

.bt-nav-item span{
  font-size:36px;
  filter:drop-shadow(0 0 10px rgba(255,255,255,.08));
}

.bt-nav-item b{
  font-size:clamp(20px,4vw,40px);
  line-height:1;
  font-weight:850;
}

.bt-nav-item.is-active{
  color:#fff;
  background:linear-gradient(180deg,rgba(255,49,89,.16),rgba(255,49,89,.05));
  box-shadow:inset 0 0 38px rgba(255,49,89,.18);
}

.bt-nav-item.is-active::before{
  content:"";
  position:absolute;
  left:0;
  right:0;
  top:0;
  height:4px;
  background:var(--bt-red);
  box-shadow:0 0 22px var(--bt-red);
}

.bt-nav-item.is-active span{
  filter:drop-shadow(0 0 16px rgba(255,49,89,.72));
}

.bt-bridge-pill{
  position:absolute;
  right:12px;
  bottom:calc(126px + env(safe-area-inset-bottom));
  z-index:70;
  padding:7px 10px;
  border-radius:999px;
  background:rgba(3,5,17,.62);
  border:1px solid rgba(123,130,210,.28);
  color:#aeb3d0;
  font-size:11px;
  font-weight:800;
  backdrop-filter:blur(8px);
}

.bt-toast{
  position:absolute;
  left:50%;
  bottom:calc(142px + env(safe-area-inset-bottom));
  z-index:80;
  transform:translateX(-50%);
  max-width:min(520px,calc(100% - 32px));
  padding:12px 16px;
  border-radius:18px;
  background:rgba(6,8,24,.86);
  border:1px solid rgba(255,49,89,.35);
  color:#fff;
  font-size:16px;
  font-weight:800;
  text-align:center;
  box-shadow:0 12px 30px rgba(0,0,0,.35),0 0 24px rgba(255,49,89,.12);
  backdrop-filter:blur(10px);
}

.bt-toast:not([hidden]){
  animation:btToastIn .22s ease-out both;
}

@keyframes btPortalPulse{
  0%,100%{opacity:.78;transform:translate(-50%,-50%) scale(.96)}
  50%{opacity:1;transform:translate(-50%,-50%) scale(1.05)}
}

@keyframes btPortalSpin{
  to{rotate:360deg}
}

@keyframes btIconFloat{
  0%,100%{transform:translateY(0) rotate(-1deg)}
  50%{transform:translateY(-10px) rotate(1deg)}
}

@keyframes btHeartPulse{
  0%,100%{opacity:.64;transform:translate(-50%,-50%) scale(.88)}
  45%{opacity:1;transform:translate(-50%,-50%) scale(1.12)}
  62%{opacity:.8;transform:translate(-50%,-50%) scale(1)}
}

@keyframes btEnergyFlow{
  to{stroke-dashoffset:-136}
}

@keyframes btEnergyPulse{
  0%,100%{opacity:.4}
  50%{opacity:1}
}

@keyframes btFogBack{
  from{transform:translate3d(-1.5%,0,0)}
  to{transform:translate3d(1.5%,1%,0)}
}

@keyframes btFogFront{
  from{transform:translate3d(2%,0,0) scale(1)}
  to{transform:translate3d(-2%,1%,0) scale(1.03)}
}

@keyframes btToastIn{
  from{opacity:0;transform:translateX(-50%) translateY(12px) scale(.96)}
  to{opacity:1;transform:translateX(-50%) translateY(0) scale(1)}
}

@media(max-width:720px){
  .bt-topbar{
    padding-left:14px;
    padding-right:14px;
  }

  .bt-profile{
    gap:9px;
  }

  .bt-shards{
    min-width:118px;
    padding:8px 11px;
    border-radius:18px;
    column-gap:8px;
  }

  .bt-shards img{
    width:38px;
    height:38px;
  }

  .bt-shards span{
    font-size:28px;
  }

  .bt-shards small{
    font-size:13px;
  }

  .bt-avatar{
    width:62px;
    height:62px;
    border-radius:18px;
  }

  .bt-avatar img{
    border-radius:14px;
  }

  .bt-hotspot{
    width:168px;
    min-height:124px;
  }

  .bt-hotspot-icon{
    width:86px;
    height:86px;
    margin-bottom:4px;
  }

  .bt-portal{
    width:108px;
    height:108px;
    border-width:6px;
  }

  .is-heart .bt-hotspot-icon{
    width:128px;
    height:128px;
  }

  .bt-heart-pulse{
    width:150px;
    height:150px;
  }

  .bt-nav{
    min-height:88px;
  }

  .bt-nav-item span{
    font-size:26px;
  }

  .bt-bridge-pill{
    display:none;
  }
}

@media(max-width:460px){
  .bt-layer,
  .bt-layer-bg,
  .bt-layer-tower{
    inset:-10% -36%;
  }

  .bt-topbar{
    gap:8px;
  }

  .bt-title h1{
    font-size:34px;
  }

  .bt-title p{
    margin-top:7px;
    font-size:20px;
  }

  .bt-shards{
    min-width:104px;
  }

  .bt-shards small{
    display:none;
  }

  .bt-avatar{
    width:56px;
    height:56px;
  }

  .hotspot-melody{left:25%;top:28%}
  .hotspot-words{left:58%;top:36%}
  .hotspot-oracle{left:24%;top:53%}
  .hotspot-heart{left:50%;top:61%}
  .hotspot-arena{left:74%;top:56%}
  .hotspot-fame{left:43%;top:74%}
  .hotspot-daily{left:14%;top:78%}
  .hotspot-chest{left:85%;top:78%}
  .hotspot-collection{left:51%;top:89%}
}

@media(prefers-reduced-motion:reduce){
  .bt-portal,
  .bt-hotspot-icon,
  .bt-heart-pulse,
  .bt-energy-line,
  .bt-layer-fog-back,
  .bt-layer-fog-front{
    animation:none!important;
  }

  .bt-particles{
    display:none;
  }
}

//=================================================
// FILE: /previews/tournaments-wall.html
<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="color-scheme" content="dark">
  <meta name="theme-color" content="#060b14">
  <title>Preview · Секция Турниры</title>
  <style>
    :root{
      --lab-bg:#050910;
      --lab-panel:#0b1422;
      --tw-ink:#eaf5ff;
      --tw-muted:#88a9cc;
      --tw-blue:#4daaff;
      --tw-cyan:#78e9ff;
      --tw-glass:#17324d;
      --tw-deep:#07101c;
      --tw-shadow:#02050a;
      --tw-red:#ff2d55;
      --tw-gold:#ffd66b;
      --tw-violet:#9a6bff;
      --tw-green:#61ffbd;
    }

    *{box-sizing:border-box}

    html,
    body{
      min-height:100%;
      margin:0;
      background:
        radial-gradient(circle at 50% 0%,rgba(77,170,255,.14),transparent 34%),
        linear-gradient(180deg,#07101c 0%,#03060b 100%);
      color:var(--tw-ink);
      font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
      overflow-x:hidden;
    }

    body{
      padding:env(safe-area-inset-top) 12px env(safe-area-inset-bottom);
    }

    .lab-page{
      width:100%;
      max-width:980px;
      margin:0 auto;
      padding:18px 0 28px;
    }

    .lab-header{
      display:flex;
      align-items:flex-end;
      justify-content:space-between;
      gap:16px;
      margin-bottom:16px;
    }

    .lab-kicker{
      font-size:11px;
      font-weight:950;
      letter-spacing:1.2px;
      text-transform:uppercase;
      color:var(--tw-cyan);
      text-shadow:0 0 14px rgba(120,233,255,.28);
    }

    .lab-title{
      margin:4px 0 0;
      font-size:28px;
      line-height:1;
      text-shadow:0 2px 0 #000,0 0 20px rgba(77,170,255,.25);
    }

    .lab-note{
      max-width:420px;
      padding:10px 12px;
      border:1px solid rgba(120,233,255,.16);
      border-radius:14px;
      background:rgba(6,12,22,.72);
      color:#9bb8d8;
      font-size:12px;
      line-height:1.45;
      box-shadow:inset 0 0 18px rgba(77,170,255,.05);
    }

    .lab-stage{
      position:relative;
      width:100%;
      min-height:640px;
      display:grid;
      place-items:center;
      padding:22px;
      border:1px solid rgba(120,233,255,.12);
      border-radius:28px;
      background:
        linear-gradient(90deg,rgba(255,45,85,.05),transparent 24%,rgba(77,170,255,.07) 50%,transparent 76%,rgba(255,45,85,.05)),
        radial-gradient(circle at 50% 50%,rgba(120,233,255,.09),transparent 38%),
        rgba(2,5,10,.64);
      box-shadow:0 22px 60px rgba(0,0,0,.44),inset 0 0 80px rgba(77,170,255,.04);
      overflow:hidden;
    }

    .lab-stage::before{
      content:"";
      position:absolute;
      inset:0;
      background:
        linear-gradient(rgba(255,255,255,.035) 1px,transparent 1px),
        linear-gradient(90deg,rgba(255,255,255,.035) 1px,transparent 1px);
      background-size:40px 40px;
      opacity:.16;
      pointer-events:none;
    }

    .lab-stage::after{
      content:"";
      position:absolute;
      left:50%;
      top:22px;
      bottom:22px;
      width:1px;
      background:linear-gradient(180deg,transparent,rgba(120,233,255,.26),transparent);
      opacity:.5;
      pointer-events:none;
    }

    .lab-footer{
      margin-top:14px;
      color:#6f88a8;
      font-size:12px;
      line-height:1.45;
    }

    .lab-code-label{
      display:inline-flex;
      margin-top:10px;
      padding:6px 10px;
      border-radius:999px;
      background:rgba(120,233,255,.08);
      border:1px solid rgba(120,233,255,.14);
      color:#bfeeff;
      font-size:11px;
      font-weight:900;
      letter-spacing:.6px;
      text-transform:uppercase;
    }

    /* =========================================================
       COMPONENT START: Tournament rear wall section
       Copy from .tw-wall-section to the matching CSS block.
       Prefix: tw-
       No JS, no audio, no WebGL, no storage.
       ========================================================= */

    .tw-wall-section{
      position:relative;
      width:min(900px,100%);
      aspect-ratio:16 / 9;
      min-height:420px;
      border-radius:26px;
      overflow:hidden;
      isolation:isolate;
      background:
        radial-gradient(circle at 50% 8%,rgba(120,233,255,.22),transparent 30%),
        linear-gradient(180deg,#122640 0%,#0d1a2c 42%,#060b13 100%);
      border:1px solid rgba(120,233,255,.24);
      box-shadow:
        0 26px 70px rgba(0,0,0,.58),
        inset 0 0 80px rgba(77,170,255,.11);
    }

    .tw-wall-section::before{
      content:"";
      position:absolute;
      inset:0;
      z-index:1;
      background:
        radial-gradient(circle at 18% 22%,rgba(120,233,255,.15),transparent 26%),
        radial-gradient(circle at 82% 18%,rgba(154,107,255,.12),transparent 28%),
        radial-gradient(circle at 50% 96%,rgba(255,45,85,.12),transparent 30%),
        linear-gradient(90deg,rgba(3,7,14,.72),transparent 18%,transparent 82%,rgba(3,7,14,.72));
      pointer-events:none;
    }

    .tw-wall-section::after{
      content:"";
      position:absolute;
      inset:0;
      z-index:18;
      background:
        repeating-linear-gradient(58deg,transparent 0 54px,rgba(255,255,255,.045) 55px,transparent 58px),
        repeating-linear-gradient(-58deg,transparent 0 68px,rgba(120,233,255,.04) 69px,transparent 72px);
      mix-blend-mode:screen;
      opacity:.64;
      pointer-events:none;
    }

    .tw-back-glow{
      position:absolute;
      inset:-20%;
      z-index:0;
      background:
        radial-gradient(ellipse at 50% 0%,rgba(120,233,255,.22),transparent 38%),
        radial-gradient(ellipse at 18% 90%,rgba(255,45,85,.16),transparent 30%),
        radial-gradient(ellipse at 84% 78%,rgba(77,170,255,.18),transparent 34%);
      filter:blur(10px);
      animation:twAurora 12s ease-in-out infinite alternate;
    }

    .tw-crystal-grid{
      position:absolute;
      inset:-2%;
      z-index:2;
      opacity:.78;
      background:
        linear-gradient(115deg,transparent 0 12%,rgba(120,233,255,.18) 12.3%,transparent 12.8% 31%,rgba(120,233,255,.12) 31.4%,transparent 31.9% 100%),
        linear-gradient(64deg,transparent 0 18%,rgba(255,255,255,.11) 18.2%,transparent 18.7% 46%,rgba(120,233,255,.13) 46.4%,transparent 46.9% 100%),
        linear-gradient(-42deg,transparent 0 22%,rgba(120,233,255,.1) 22.2%,transparent 22.8% 63%,rgba(255,255,255,.08) 63.3%,transparent 63.8% 100%);
      mask-image:linear-gradient(180deg,#000 0%,#000 82%,transparent 100%);
      pointer-events:none;
    }

    .tw-dome{
      position:absolute;
      left:-10%;
      right:-10%;
      top:-20%;
      height:46%;
      z-index:3;
      border-radius:0 0 50% 50%;
      background:
        radial-gradient(ellipse at 50% 100%,rgba(120,233,255,.2),transparent 55%),
        repeating-conic-gradient(from 7deg at 50% 116%,rgba(120,233,255,.18) 0 1deg,transparent 1deg 13deg);
      border-bottom:2px solid rgba(120,233,255,.22);
      box-shadow:0 18px 38px rgba(0,0,0,.28),inset 0 -18px 36px rgba(120,233,255,.05);
      pointer-events:none;
    }

    .tw-dome-ribs{
      position:absolute;
      left:-8%;
      right:-8%;
      top:0;
      height:32%;
      z-index:4;
      background:
        repeating-linear-gradient(23deg,transparent 0 42px,rgba(120,233,255,.18) 43px,rgba(120,233,255,.18) 46px,transparent 47px 96px),
        repeating-linear-gradient(-23deg,transparent 0 48px,rgba(3,8,16,.78) 49px,rgba(3,8,16,.78) 54px,transparent 55px 102px);
      opacity:.82;
      mask-image:linear-gradient(180deg,#000 0%,#000 76%,transparent 100%);
      pointer-events:none;
    }

    .tw-main-wall{
      position:absolute;
      left:5%;
      right:5%;
      bottom:8%;
      height:70%;
      z-index:5;
      border-radius:34px 34px 16px 16px;
      background:
        linear-gradient(180deg,rgba(41,67,96,.72),rgba(8,15,26,.96)),
        repeating-linear-gradient(90deg,transparent 0 82px,rgba(120,233,255,.07) 83px,transparent 86px);
      border:1px solid rgba(120,233,255,.18);
      box-shadow:
        0 20px 50px rgba(0,0,0,.44),
        inset 0 0 40px rgba(120,233,255,.08);
    }

    .tw-main-wall::before{
      content:"";
      position:absolute;
      inset:16px 18px 20px;
      border-radius:28px 28px 12px 12px;
      background:
        repeating-linear-gradient(0deg,rgba(0,0,0,.2) 0 16px,rgba(255,255,255,.025) 17px 18px),
        linear-gradient(90deg,rgba(4,8,15,.55),transparent 20%,transparent 80%,rgba(4,8,15,.55));
      border:1px solid rgba(255,255,255,.045);
      opacity:.82;
    }

    .tw-main-wall::after{
      content:"";
      position:absolute;
      left:50%;
      bottom:-4%;
      width:84%;
      height:24%;
      transform:translateX(-50%);
      border-radius:50%;
      background:radial-gradient(ellipse,rgba(120,233,255,.16),transparent 72%);
      filter:blur(8px);
      pointer-events:none;
    }

    .tw-column{
      position:absolute;
      top:26%;
      bottom:8%;
      width:7.8%;
      z-index:9;
      background:
        linear-gradient(90deg,rgba(4,8,15,.94),rgba(70,103,135,.86) 34%,rgba(155,210,235,.28) 50%,rgba(42,72,104,.9) 68%,rgba(3,7,13,.96));
      border:1px solid rgba(120,233,255,.15);
      border-radius:18px 18px 8px 8px;
      box-shadow:
        0 14px 26px rgba(0,0,0,.38),
        inset 0 0 18px rgba(120,233,255,.08);
    }

    .tw-column::before,
    .tw-column::after{
      content:"";
      position:absolute;
      left:-14%;
      right:-14%;
      height:12%;
      border-radius:14px;
      background:
        linear-gradient(180deg,rgba(106,148,184,.9),rgba(14,24,38,.95));
      border:1px solid rgba(120,233,255,.16);
      box-shadow:0 8px 18px rgba(0,0,0,.28);
    }

    .tw-column::before{top:-5%}
    .tw-column::after{bottom:-4%}

    .tw-column-left{left:6%}
    .tw-column-mid-left{left:29%}
    .tw-column-mid-right{right:29%}
    .tw-column-right{right:6%}

    .tw-arch{
      position:absolute;
      bottom:13%;
      width:25%;
      height:58%;
      z-index:8;
    }

    .tw-arch-left{left:11%}
    .:0;
      z-index:21;
      pointer-events:none;
    }

    .tw-sparks i{
      position:absolute;
      width:4px;
      height:4px;
      border-radius:50%;
      background:var(--tw-gold);
      box-shadow:0 0 12px var(--tw-gold);
      animation:twSpark 2.8s ease-in-out infinite;
    }

    .tw-sparks i:nth-child(1){left:44%;top:34%;animation-delay:.1s}
    .tw-sparks i:nth-child(2){left:57%;top:30%;animation-delay:.7s}
    .tw-sparks i:nth-child(3){left:66%;top:46%;animation-delay:1.2s}
    .tw-sparks i:nth-child(4){left:37%;top:51%;animation-delay:1.8s}
    .tw-sparks i:nth-child(5){left:53%;top:62%;animation-delay:2.2s}

    .tw-floor-lip{
      position:absolute;
      left:-4%;
      right:-4%;
      bottom:-10%;
      height:28%;
      z-index:12;
      background:
        radial-gradient(ellipse at 50% 0%,rgba(120,233,255,.24),transparent 58%),
        repeating-linear-gradient(90deg,rgba(255,255,255,.08) 0 2px,transparent 2px 58px),
        linear-gradient(180deg,rgba(75,120,160,.34),rgba(3,6,10,.94));
      transform:perspective(360px) rotateX(58deg);
      transform-origin:top center;
      border-top:2px solid rgba(120,233,255,.24);
      box-shadow:inset 0 18px 46px rgba(120,233,255,.08);
      pointer-events:none;
    }

    .tw-depth-shadow{
      position:absolute;
      left:8%;
      right:8%;
      bottom:5%;
      height:20%;
      z-index:7;
      background:radial-gradient(ellipse,rgba(0,0,0,.72),transparent 70%);
      filter:blur(12px);
      pointer-events:none;
    }

    @keyframes twAurora{
      0%{transform:translate3d(-1.5%,0,0) scale(1)}
      100%{transform:translate3d(1.5%,1%,0) scale(1.04)}
    }

    @keyframes twRing{
      to{transform:translate(-50%,-50%) rotate(360deg)}
    }

    @keyframes twSpark{
      0%,100%{opacity:.18;transform:translateY(0) scale(.8)}
      50%{opacity:1;transform:translateY(-8px) scale(1.15)}
    }

    /* =========================================================
       COMPONENT END
       ========================================================= */

    @media(max-width:720px){
      .lab-header{
        align-items:flex-start;
        flex-direction:column;
      }

      .lab-stage{
        min-height:560px;
        padding:14px;
      }

      .tw-wall-section{
        aspect-ratio:9 / 13;
        min-height:520px;
      }

      .tw-main-wall{
        left:4%;
        right:4%;
        bottom:8%;
        height:73%;
      }

      .tw-dome{
        left:-42%;
        right:-42%;
      }

      .tw-dome-ribs{
        left:-44%;
        right:-44%;
      }

      .tw-arch{
        bottom:15%;
        width:33%;
        height:52%;
      }

      .tw-arch-left{
        left:4%;
        transform:scale(.86);
        transform-origin:bottom left;
      }

      .tw-arch-right{
        right:4%;
        transform:scale(.86);
        transform-origin:bottom right;
      }

      .tw-arch-center{
        width:42%;
        height:61%;
      }

      .tw-column{
        width:9.5%;
      }

      .tw-column-left{left:2%}
      .tw-column-mid-left{left:25%}
      .tw-column-mid-right{right:25%}
      .tw-column-right{right:2%}

      .tw-side-caption{
        font-size:10px;
      }

      .tw-center-caption{
        font-size:13px;
      }

      .tw-floor-lip{
        bottom:-8%;
        height:24%;
      }
    }

    @media(max-width:420px){
      body{
        padding-left:8px;
        padding-right:8px;
      }

      .lab-title{
        font-size:24px;
      }

      .lab-stage{
        border-radius:20px;
      }

      .tw-wall-section{
        border-radius:22px;
      }
    }

    @media(prefers-reduced-motion:reduce){
      .tw-back-glow,
      .tw-energy-ring,
      .tw-sparks i{
        animation:none;
      }
    }
  </style>
</head>
<body>
  <main class="lab-page">
    <header class="lab-header">
      <div>
        <div class="lab-kicker">Preview Lab · Game Center Asset</div>
        <h1 class="lab-title">Задняя секция стены · Турниры</h1>
      </div>
      <div class="lab-note">
        Основную комнату не трогаем. Здесь изолированно доводим один графический блок:
        задняя стена, арочные витрины, центральная табличка «ТУРНИРЫ».
      </div>
    </header>

    <section class="lab-stage" aria-label="Лаборатория просмотра секции Турниры">
      <!-- COMPONENT START: Tournament rear wall section -->
      <section class="tw-wall-section" aria-label="Секция стены Турниры">
        <div class="tw-back-glow"></div>
        <div class="tw-crystal-grid"></div>
        <div class="tw-dome"></div>
        <div class="tw-dome-ribs"></div>

        <div class="tw-main-wall"></div>
        <div class="tw-depth-shadow"></div>

        <div class="tw-column tw-column-left"></div>
        <div class="tw-column tw-column-mid-left"></div>
        <div class="tw-column tw-column-mid-right"></div>
        <div class="tw-column tw-column-right"></div>

        <article class="tw-arch tw-arch-left">
          <div class="tw-arch-frame"></div>
          <div class="tw-arch-title">Рейтинг</div>
          <div class="tw-side-icon">📊</div>
          <div class="tw-score-lines">
            <i></i><i></i><i></i><i></i>
          </div>
          <div class="tw-side-caption">Лиги<br>игроков</div>
        </article>

        <article class="tw-arch tw-arch-center">
          <div class="tw-arch-frame"></div>
          <div class="tw-arch-title">Турниры</div>
          <div class="tw-door"></div>
          <div class="tw-energy-ring"></div>
          <div class="tw-tournament-badge">🏆</div>
          <div class="tw-center-caption">Арены<br>сердец</div>
          <div class="tw-sparks">
            <i></i><i></i><i></i><i></i><i></i>
          </div>
        </article>

        <article class="tw-arch tw-arch-right">
          <div class="tw-arch-frame"></div>
          <div class="tw-arch-title">Призы</div>
          <div class="tw-side-icon">🎁</div>
          <div class="tw-side-caption">Награды<br>сезона</div>
        </article>

        <div class="tw-floor-lip"></div>
      </section>
      <!-- COMPONENT END -->
    </section>

    <footer class="lab-footer">
      <div class="lab-code-label">copy-safe component</div>
      <p>
        Для переноса в общую комнату потом копируем только блок
        <b>.tw-wall-section</b> и CSS-секции с префиксом <b>tw-</b>.
        Сейчас это полностью автономный HTML/CSS preview без JS, audio, WebGL и storage.
      </p>
    </footer>
  </main>
</body>
</html>

